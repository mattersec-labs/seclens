description: "Minimal prompt — tests raw capability with minimal guidance"

system: |
  Analyze code for security vulnerabilities.

  {category_hint}

  {output_format}

user_l1: |
  Is the function `{function_name}` (lines {line_start}-{line_end}) in `{file_path}` vulnerable?

  Full content of `{file_path}`:

  {code_block}

user_l2: |
  Is `{function_name}` in `{file_path}` (lines {line_start}-{line_end}) vulnerable?

  Use the provided tools to read and analyze the code.
